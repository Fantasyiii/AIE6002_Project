# VibeMatch 技术栈

## 项目概述
VibeMatch 是一个基于 RAG（Retrieval-Augmented Generation）的语义化电影推荐引擎，用户可通过自然语言描述"感觉"来搜索电影，系统返回精准推荐并解释匹配原因。

---

## 核心技术栈

| 层级 | 技术选型 | 用途说明 |
|:---|:---|:---|
| **前端** | Next.js 15 + React 19 + TypeScript | 聊天界面，展示推荐结果与溯源卡片 |
| **样式** | TailwindCSS | 响应式 UI 样式 |
| **后端 API** | FastAPI + Uvicorn | Python 异步 REST API，提供 `/chat` 及 Baseline 端点 |
| **RAG 框架** | LangChain 0.2.x (LCEL) | 编排 RAG 流程：检索链、Prompt 组装、LLM 调用 |
| **向量数据库** | ChromaDB 1.5.x | 本地持久化向量存储，预编译 wheel，无需 C++ 编译器 |
| **Embedding 模型** | `all-MiniLM-L6-v2`（本地） | HuggingFace 模型，384 维向量，免 API 费用 |
| **LLM（主）** | DeepSeek（`deepseek-v4-flash`） | 通过 `DEEPSEEK_API_KEY` 配置，OpenAI 兼容接口 |
| **LLM（备选）** | OpenAI GPT-4o-mini | 通过 `OPENAI_API_KEY` 配置，自动回退 |
| **数据** | TMDB 5000 Movie Dataset | 4799 部电影的标题、类型、年份、剧情概述 |

---

## 详细组件与依赖

### 1. 数据层
| 组件 | 技术 | 说明 |
|:---|:---|:---|
| 数据集 | TMDB 5000 Movie Dataset (Kaggle) | `tmdb_5000_movies.csv` + `tmdb_5000_credits.csv` |
| 数据处理 | Pandas (`data_processor.py`) | 清洗、解析 JSON 列、拼接富文本字段 |
| 输出 | `backend/data/movies_processed.json` | 4799 条结构化电影记录 |

### 2. 向量化与检索层
| 组件 | 技术 | 说明 |
|:---|:---|:---|
| Embedding 模型 | `HuggingFaceEmbeddings` (`all-MiniLM-L6-v2`) | 本地模型，保存至 `backend/models/` |
| 向量存储 | ChromaDB 1.5.x | 持久化至 `backend/chroma_db/`，支持 metadata 过滤 |
| 检索策略 | Cosine Similarity | 默认检索模式 |
| 检索策略 | MMR（Maximal Marginal Relevance） | 平衡相关性与推荐多样性 |
| 检索参数 | Top-K = 5 | 默认返回 5 个候选电影 |

### 3. 生成层
| 组件 | 技术 | 说明 |
|:---|:---|:---|
| LLM 初始化 | `get_llm()` (`rag_chain.py`) | 优先 DeepSeek，回退 OpenAI，绕过本地代理 |
| Prompt 模板 | `ChatPromptTemplate` (LangChain) | 严格限制 LLM 只推荐 Context 中出现的电影，防幻觉 |
| RAG Chain | LCEL `RunnableParallel` + `RunnableLambda` | 并行执行检索和 Passthrough，组装上下文 |
| 输出解析器 | `StrOutputParser` | 解析 LLM 返回的文本 |

### 4. API 层
| 组件 | 技术 | 说明 |
|:---|:---|:---|
| Web 框架 | FastAPI 0.111.x | 异步 REST API，自动生成 OpenAPI 文档 |
| ASGI 服务器 | Uvicorn 0.30.x | 生产级异步服务器 |
| CORS | `CORSMiddleware` | 允许 `localhost:3000`（Next.js 开发服务器）跨域 |
| 主端点 | `POST /chat` | 完整 RAG 推荐，支持 similarity / mmr 模式切换 |
| 基线端点 | `POST /baseline/pure-llm` | Pure-LLM 基线（无检索） |
| 基线端点 | `POST /baseline/retrieval-only` | 仅向量检索，无 LLM 生成 |

### 5. 前端层
| 组件 | 技术 | 说明 |
|:---|:---|:---|
| 框架 | Next.js 15.3.2 (App Router) | 基于 Movies++ 改造，移除 Langflow/RSC 依赖 |
| 状态管理 | React Hooks (`useMovieSearch`) | 管理消息列表、加载状态、错误处理 |
| 渲染 | Markdown 渲染 + SourceCard 组件 | 左侧助手消息 + 可展开的来源电影卡片 |
| 构建 | `npm run build` → `.next/` | 静态预渲染，生产就绪 |

---

## 环境配置

### `backend/requirements.txt`（实际版本）
```txt
# Core RAG Framework
langchain==0.2.17
langchain-community==0.2.19
langchain-openai==0.1.25

# Vector Store (pre-compiled wheel, no C++ compiler needed)
chromadb>=0.5.0

# Embeddings (Local)
sentence-transformers~=2.7.0

# API Framework
fastapi~=0.111.0
uvicorn~=0.30.0
python-multipart~=0.0.9

# Data Processing
pandas~=2.1.0
numpy>=1.26,<2

# Utilities
python-dotenv~=1.0.0
tiktoken~=0.7.0
requests~=2.32.0
```

### 环境变量 (`backend/.env`)
```bash
# 主 LLM（DeepSeek，推荐）
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1   # 可选，默认值
DEEPSEEK_MODEL=deepseek-v4-flash                 # 可选，默认值

# 备选 LLM（OpenAI，DEEPSEEK_API_KEY 未设置时生效）
OPENAI_API_KEY=your_openai_api_key_here
```

---

## 技术选型理由

1. **ChromaDB 1.5.x 替代 FAISS**：内置持久化和 metadata 过滤，预编译 wheel 无需 C++ 编译器，与 LangChain 集成更简洁
2. **all-MiniLM-L6-v2 作为 Embedding**：本地运行零费用，384 维向量存储高效，语义相似度任务表现足够
3. **DeepSeek 作为主 LLM**：成本低、响应速度快，通过 OpenAI 兼容接口无缝接入 LangChain；OpenAI 作自动回退
4. **FastAPI 替代 Streamlit**：解耦前后端，支持独立扩展；前端用 Next.js 提供更丰富的交互体验
5. **LCEL（LangChain Expression Language）**：声明式链式调用，`RunnableParallel` 并行执行检索与 Passthrough，代码简洁可维护
